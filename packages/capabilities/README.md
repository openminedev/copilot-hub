# Capabilities

Capability modules are feature units that can be enabled per agent.

Examples:
- telegram-image-input
- tool-routing
- custom-domain-knowledge

Guidelines:
- no direct mutation of engine core
- explicit manifest + permissions
- deterministic hooks
